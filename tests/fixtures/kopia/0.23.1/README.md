# Kopia 0.23.1 JSON fixtures

Captured 2026-09-04 from `kopia 0.23.1` against a scratch filesystem repository, with paths, host and user scrubbed. Regenerate with `scripts/capture-kopia-fixtures.sh` when the pinned version changes and diff the result (spec §5).
